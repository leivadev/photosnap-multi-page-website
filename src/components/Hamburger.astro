---
import Button1 from './Button1.astro'
import Lines from '../icons/lines.astro'
import Close from '../icons/close.astro'
---

<div class="hamburger">
    <Lines />
</div>
<div class="close-icon">
    <Close />
</div>
<div class="expanded">
    <nav>
        <a class="textPrimary" href="#">Stories</a>
        <a class="textPrimary" href="#">Features</a>
        <a class="textPrimary" href="#">Pricing</a>
    </nav>
    <Button1 type="button" mode='dark' text="GET AN INVITE" />
</div>

<script>
    const hamburger = document.querySelector('.hamburger') as HTMLElement;
    const expanded = document.querySelector('.expanded') as HTMLElement;
    const closeBtn = document.querySelector('.close-icon') as HTMLElement;

    const showExpandedMenu = () => {
        expanded.style.display = 'flex';
        closeBtn.style.display = 'block';
        hamburger.style.display = 'none';
    };

    const hideExpandedMenu = () => {
        expanded.style.display = 'none';
        closeBtn.style.display = 'none';
        hamburger.style.display = 'block';
    };

    const handleResize = () => {
        if (window.innerWidth >= 481) {
            hamburger.style.display = 'none';
            expanded.style.display = 'none';
        } else {
            hamburger.style.display = 'block';
            hideExpandedMenu();
        }
    };

    hamburger.addEventListener('click', showExpandedMenu);
    closeBtn.addEventListener('click', hideExpandedMenu);
    window.addEventListener('resize', handleResize);

    handleResize();
</script>

<style>
    .hamburger {
        cursor: pointer;
    }

    .expanded {
        display: none;
    }

    .close-icon {
        display: none;
        cursor: pointer;
    }

    .expanded {
        flex-direction: column;
        justify-content: center;
        gap: 1rem;

        position: absolute;
        top: 71px;
        left: 0;
        background-color: var(--white-color);
        width: 100vw;
        padding: 1rem;
    }

    nav {
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.25);
        text-align: center;
    }

    .textPrimary {
        color: var(--black-color);
        font-size: 0.75rem;
    }

    .textPrimary:hover {
        opacity: 0.3;
    }

</style>